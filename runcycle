# Array of values
#dims=(50 100 200)

declare -a deltas=("4." "5." "6.")
declare -a dims=("10" "20" "30")

for j in ${dims[@]}; do
	for i in ${deltas[@]}; do
            	./main_martina3 $j $i
        done
done
